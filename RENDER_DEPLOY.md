# 🎨 Деплой на Render.com

Пошаговая инструкция для развертывания AI-Редактора Журнала на Render с единым сервисом.

## ✨ Преимущества единого сервиса

- 💰 **Дешевле** - один сервис вместо двух
- 🚀 **Быстрее** - нет задержек между frontend/backend
- 🔧 **Проще** - одна конфигурация
- 📦 **Монолит** - всё в одном контейнере

## 🏗️ Архитектура

```
┌─────────────────────────────────────────┐
│      Render Web Service (Docker)        │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────┐         ┌──────────────┐  │
│  │  Nginx   │────────>│   FastAPI    │  │
│  │  (Port)  │         │   Backend    │  │
│  │          │         │   (:8000)    │  │
│  └──────────┘         └──────────────┘  │
│       │                                 │
│       └─> Serves React Frontend        │
│                                         │
└─────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────┐
│   PostgreSQL (Free)     │
└─────────────────────────┘
           │
           ▼
┌─────────────────────────┐
│   Redis (Free)          │
└─────────────────────────┘
```

## 🚀 Метод 1: Через Blueprint (Рекомендуется)

### Шаг 1: Подготовка

Убедитесь, что все файлы закоммичены:

```bash
git add .
git commit -m "Add Render deployment configuration"
git push origin main
```

### Шаг 2: Создание сервиса

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"New +"** → **"Blueprint"**
3. Подключите ваш GitHub репозиторий
4. Render автоматически обнаружит `render.yaml`
5. Нажмите **"Apply"**

### Шаг 3: Настройка переменных окружения

После создания сервиса, добавьте:

**Environment Variables:**
```env
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-здесь
AI_MODEL=deepseek/deepseek-chat
```

Все остальные переменные уже настроены в `render.yaml`!

### Шаг 4: Деплой

Render автоматически:
1. ✅ Создаст PostgreSQL базу данных
2. ✅ Создаст Redis instance
3. ✅ Соберет Docker образ
4. ✅ Запустит приложение

---

## 🚀 Метод 2: Ручное создание

### Шаг 1: Создать PostgreSQL

1. **New +** → **PostgreSQL**
2. Name: `ai-editor-db`
3. Database: `aieditor`
4. User: `aieditor`
5. Region: выберите ближайший
6. Plan: **Free**
7. Create Database

### Шаг 2: Создать Redis

1. **New +** → **Redis**
2. Name: `ai-editor-redis`
3. Region: тот же, что и PostgreSQL
4. Plan: **Free**
5. Create Redis

### Шаг 3: Создать Web Service

1. **New +** → **Web Service**
2. Подключите GitHub репозиторий
3. Name: `ai-journal-editor`
4. Region: тот же
5. Branch: `main`
6. **Environment**: **Docker**
7. **Dockerfile Path**: `./Dockerfile.render`
8. Plan: **Free** (или Starter для production)

### Шаг 4: Настроить переменные окружения

```env
# Базы данных (автоматически из Render)
DATABASE_URL=${{ai-editor-db.DATABASE_URL}}
REDIS_URL=${{ai-editor-redis.REDIS_URL}}

# AI
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ
AI_MODEL=deepseek/deepseek-chat

# Настройки приложения
SESSION_TTL_HOURS=24
MAX_FILE_SIZE_MB=50
MAX_ARTICLES_PER_SESSION=100
PORT=10000
UPLOAD_DIR=/app/uploads
```

### Шаг 5: Deploy

1. Нажмите **"Create Web Service"**
2. Render начнет сборку (5-10 минут)
3. После деплоя откройте URL

---

## 🔐 Переменные окружения

### Обязательные

| Переменная | Где взять | Пример |
|------------|-----------|--------|
| `OPENROUTER_API_KEY` | [openrouter.ai/keys](https://openrouter.ai/keys) | `sk-or-v1-...` |
| `DATABASE_URL` | Render PostgreSQL | Автоматически |
| `REDIS_URL` | Render Redis | Автоматически |

### Опциональные

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `AI_MODEL` | `deepseek/deepseek-chat` | Модель AI |
| `SESSION_TTL_HOURS` | `24` | Время жизни сессии |
| `MAX_FILE_SIZE_MB` | `50` | Макс размер файла |
| `MAX_ARTICLES_PER_SESSION` | `100` | Макс статей |

---

## 📊 Структура файлов для Render

```
autoredactor/
├── render.yaml                 # Blueprint конфигурация
├── Dockerfile.render          # Unified Dockerfile
├── start.render.sh           # Startup script
├── nginx.render.conf         # Nginx конфигурация
├── backend/                  # FastAPI приложение
└── frontend/                 # React приложение
```

---

## 🎯 После деплоя

Ваше приложение будет доступно по адресу:

```
https://ai-journal-editor.onrender.com
```

**API документация:**
```
https://ai-journal-editor.onrender.com/api/docs
```

---

## 🐛 Troubleshooting

### 1. Сервис не запускается

**Проверьте логи:**
```
Render Dashboard → Service → Logs
```

**Частые проблемы:**
- LibreOffice не установлен → уже в Dockerfile
- Порт неправильный → используйте `$PORT`
- База не подключена → проверьте `DATABASE_URL`

### 2. Frontend показывает белый экран

**Проверьте:**
1. Build прошел успешно (см. логи)
2. Nginx правильно настроен
3. Файлы в `/app/frontend/dist`

**Решение:**
```bash
# В логах должно быть:
✅ Frontend built successfully
✅ Nginx started
```

### 3. Backend не отвечает

**Проверьте:**
1. Health check endpoint: `/health`
2. Логи FastAPI
3. DATABASE_URL правильный

**Тест:**
```bash
curl https://your-app.onrender.com/health
# Должно вернуть: {"status":"healthy"}
```

### 4. Долгая сборка

**Нормально!** Первая сборка может занять:
- 5-10 минут (установка LibreOffice)
- 2-3 минуты последующие (кэш)

### 5. LibreOffice ошибки

Если есть ошибки с конвертацией DOCX → PDF:

```bash
# Проверьте, что LibreOffice установлен
# В Dockerfile.render уже добавлен
```

---

## 💰 Стоимость на Render

### Free Plan
- **Web Service**: Free (750 часов/месяц)
- **PostgreSQL**: Free (90 дней, потом $7/месяц)
- **Redis**: Free (90 дней, потом $5/месяц)

**Итого:** Бесплатно первые 90 дней, потом ~$12/месяц

### Starter Plan
- **Web Service**: $7/месяц
- **PostgreSQL**: $7/месяц
- **Redis**: $5/месяц

**Итого:** ~$19/месяц (с лучшей производительностью)

---

## 🔄 Обновление приложения

```bash
git add .
git commit -m "Update application"
git push origin main
```

Render автоматически:
1. Обнаружит изменения
2. Пересоберет образ
3. Задеплоит новую версию

---

## 📝 Чеклист перед деплоем

- [ ] OpenRouter API ключ получен
- [ ] Все файлы закоммичены в Git
- [ ] `render.yaml` настроен
- [ ] `Dockerfile.render` присутствует
- [ ] `start.render.sh` исполняемый
- [ ] PostgreSQL база создана на Render
- [ ] Redis instance создан на Render
- [ ] Переменные окружения добавлены

---

## 🎉 Готово!

После успешного деплоя:

1. ✅ Frontend доступен по URL
2. ✅ Backend API работает
3. ✅ PostgreSQL подключена
4. ✅ Redis работает
5. ✅ AI функции активны

**Тестирование:**
1. Откройте https://your-app.onrender.com
2. Загрузите .docx файл
3. AI извлечет метаданные
4. Сгенерируйте PDF журнал

---

**Нужна помощь?**
- [Render Docs](https://render.com/docs)
- [Render Community](https://community.render.com)
- [GitHub Issues](https://github.com/m34959203/autoredactor/issues)
