# 🚂 Railway Unified Service Deployment

Деплой AI-Редактора Журнала на Railway как **единый сервис**.

## ✨ Преимущества единого сервиса

- 💰 **Дешевле** - один сервис вместо двух
- 🚀 **Быстрее** - нет задержек между frontend/backend
- 🔧 **Проще** - одна конфигурация, один деплой
- 📦 **Монолит** - всё в одном контейнере

## 🏗️ Архитектура

```
┌─────────────────────────────────────────┐
│    Railway Service (Docker)             │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────┐         ┌──────────────┐  │
│  │  Nginx   │────────>│   FastAPI    │  │
│  │  (Port)  │         │   Backend    │  │
│  │          │         │   (:8001)    │  │
│  └──────────┘         └──────────────┘  │
│       │                                 │
│       └─> Serves React Frontend        │
│                                         │
└─────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────┐
│   PostgreSQL Plugin     │
└─────────────────────────┘
           │
           ▼
┌─────────────────────────┐
│   Redis Plugin          │
└─────────────────────────┘
```

## 🚀 Быстрый старт

### Шаг 1: Подготовка

Убедитесь, что все файлы закоммичены:

```bash
git add .
git commit -m "Add Railway unified deployment"
git push origin main
```

### Шаг 2: Создать проект на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите репозиторий `autoredactor`

### Шаг 3: Добавить базы данных

#### PostgreSQL:
1. В проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу

#### Redis:
1. Нажмите **"+ New"**
2. Выберите **"Database"** → **"Add Redis"**
3. Railway автоматически создаст Redis

### Шаг 4: Настроить Web Service

Railway автоматически создаст сервис из вашего репозитория.

**Настройки сервиса:**

В Settings → Build:
- Builder: **Dockerfile**
- Dockerfile Path: `Dockerfile.railway`
- Build Command: (оставить пустым)

В Settings → Deploy:
- Start Command: `/start.sh`
- Health Check Path: `/health`

### Шаг 5: Настроить переменные окружения

**Variables:**

```env
# База данных (автоматически от Railway)
DATABASE_URL=${{Postgres.DATABASE_URL}}
REDIS_URL=${{Redis.REDIS_URL}}

# AI (добавьте вручную)
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ
AI_MODEL=deepseek/deepseek-chat

# Настройки приложения
SESSION_TTL_HOURS=24
MAX_FILE_SIZE_MB=50
MAX_ARTICLES_PER_SESSION=100
UPLOAD_DIR=/app/uploads

# Port (автоматически)
PORT=${{PORT}}
```

### Шаг 6: Generate Domain

1. В Settings → Networking
2. Нажмите **"Generate Domain"**
3. Получите URL: `https://your-app.up.railway.app`

### Шаг 7: Deploy!

Railway автоматически:
1. ✅ Соберет Docker образ
2. ✅ Установит LibreOffice
3. ✅ Соберет React Frontend
4. ✅ Настроит Nginx
5. ✅ Запустит FastAPI Backend
6. ✅ Подключит PostgreSQL и Redis

---

## 📋 Файлы конфигурации

```
autoredactor/
├── railway.toml              # Railway конфигурация
├── Dockerfile.railway        # Unified Dockerfile
├── start.railway.sh         # Startup script
├── nginx.railway.conf       # Nginx config
├── backend/                 # FastAPI приложение
└── frontend/                # React приложение
```

---

## 🔐 Переменные окружения

### Обязательные

| Переменная | Где взять | Значение |
|------------|-----------|----------|
| `DATABASE_URL` | Railway PostgreSQL | `${{Postgres.DATABASE_URL}}` |
| `REDIS_URL` | Railway Redis | `${{Redis.REDIS_URL}}` |
| `OPENROUTER_API_KEY` | [openrouter.ai](https://openrouter.ai/keys) | `sk-or-v1-...` |

### Опциональные

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `AI_MODEL` | `deepseek/deepseek-chat` | Модель AI |
| `SESSION_TTL_HOURS` | `24` | Время жизни сессии |
| `MAX_FILE_SIZE_MB` | `50` | Макс размер файла |

---

## 🎯 После деплоя

Ваше приложение будет доступно:

```
https://your-app.up.railway.app
```

**API документация:**
```
https://your-app.up.railway.app/docs
```

**Проверка здоровья:**
```
https://your-app.up.railway.app/health
```

---

## 🐛 Troubleshooting

### 1. Build fails

**Проверьте логи:**
```
Railway Dashboard → Service → Deployments → View Logs
```

**Частые проблемы:**
- LibreOffice не установлен → добавлен в Dockerfile.railway
- Нет памяти → увеличьте в Settings
- Timeout при сборке → нормально для первой сборки

### 2. Service crashes after deploy

**Проверьте:**
```bash
# В логах должно быть:
✅ Nginx started
✅ FastAPI Backend started
✅ Services started successfully!
```

**Если нет:**
- Проверьте `DATABASE_URL`
- Проверьте `OPENROUTER_API_KEY`
- Проверьте порт `$PORT`

### 3. Frontend показывает 502

**Проблема:** Nginx не может подключиться к Backend

**Решение:**
1. Проверьте, что Backend запущен (см. логи)
2. Проверьте `nginx.railway.conf` - порт должен быть 8001
3. Перезапустите сервис

### 4. API возвращает 500

**Проверьте:**
1. `DATABASE_URL` правильный
2. PostgreSQL доступна
3. Миграции выполнены

**Тест:**
```bash
curl https://your-app.up.railway.app/health
# Должно вернуть: {"status":"healthy"}
```

### 5. LibreOffice errors

**В логах:**
```
Error: LibreOffice not found
```

**Решение:**
- Уже исправлено в `Dockerfile.railway`
- Если ошибка сохраняется, пересоберите:
```
Railway Dashboard → Deployments → Redeploy
```

---

## 💰 Стоимость

### Hobby Plan
- **Web Service**: $5 credit/месяц бесплатно
- **PostgreSQL**: Включено
- **Redis**: Включено

После исчерпания кредитов:
- ~$5-10/месяц для небольших проектов

### Pro Plan
- $20/месяц за план
- Лучше производительность
- Больше ресурсов

---

## 🔄 Обновление приложения

```bash
git add .
git commit -m "Update application"
git push origin main
```

Railway автоматически:
1. Обнаружит изменения
2. Пересоберет образ
3. Задеплоит новую версию

---

## 📊 Мониторинг

**Метрики:**
```
Railway Dashboard → Service → Metrics
```

Доступно:
- CPU usage
- Memory usage
- Network traffic
- Response times

**Логи:**
```
Railway Dashboard → Service → Logs
```

Фильтры:
- Last hour / day / week
- Search by keyword
- Download logs

---

## 🎉 Готово!

После успешного деплоя:

1. ✅ **Frontend** доступен по URL
2. ✅ **Backend API** работает (`/docs`)
3. ✅ **PostgreSQL** подключена
4. ✅ **Redis** работает
5. ✅ **AI функции** активны
6. ✅ **PDF генерация** работает

**Тестирование:**
1. Откройте ваш Railway URL
2. Загрузите .docx файл
3. AI извлечет метаданные
4. Отсортируйте статьи
5. Сгенерируйте PDF журнал

---

## 📝 Сравнение с раздельным деплоем

| Аспект | Unified Service | Separate Services |
|--------|----------------|-------------------|
| **Стоимость** | $5-10/мес | $10-15/мес |
| **Простота** | ✅ Проще | ❌ Сложнее |
| **Latency** | ✅ Ниже | ⚠️ Выше |
| **Масштабирование** | ❌ Сложнее | ✅ Проще |

**Рекомендация:** Unified для малых/средних проектов

---

**Нужна помощь?**
- [Railway Docs](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [GitHub Issues](https://github.com/m34959203/autoredactor/issues)
