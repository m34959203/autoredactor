# AI-Редактор журнала v2.0

🤖 **Веб-приложение для автоматизации сборки научного журнала с AI-извлечением метаданных**

## ✨ Новое в версии 2.0

### AI-функционал
- ✅ **Автоматическое извлечение названия статьи** из .docx файлов
- ✅ **Автоматическое извлечение автора** с помощью AI
- ✅ **Определение языка** (латиница/кириллица) для правильной сортировки
- ✅ **OpenRouter.ai** интеграция с бесплатными моделями

### Управление статьями
- ✅ **Drag & Drop** загрузка файлов
- ✅ **Inline редактирование** названия и автора
- ✅ **Карточки статей** с индикацией языка [LAT]/[CYR]
- ✅ **Автоматическая сортировка** A→Z, затем А→Я
- ✅ **Удаление статей** из списка

### Архив выпусков
- ✅ **Постоянное хранение** PDF журналов
- ✅ **Фильтр по годам и месяцам**
- ✅ **Просмотр PDF** в браузере
- ✅ **Скачивание** архивных выпусков

### Технологии
- ✅ **FastAPI** backend вместо Flask
- ✅ **PostgreSQL** для хранения метаданных
- ✅ **React 19** с TypeScript
- ✅ **Tailwind CSS v4**
- ✅ **Docker Compose** для easy deployment

---

## 🚀 Быстрый старт

### 1. Требования

- Docker & Docker Compose
- OpenRouter.ai API ключ ([получить здесь](https://openrouter.ai))

### 2. Настройка

```bash
# Клонируйте репозиторий
git clone https://github.com/m34959203/autoredactor.git
cd autoredactor

# Создайте .env файл
echo "OPENROUTER_API_KEY=your-key-here" > .env

# Запустите все сервисы
docker-compose up -d
```

### 3. Откройте приложение

Приложение будет доступно по адресу: **http://localhost:3000**

API документация: **http://localhost:8000/docs**

---

## 📖 Использование

### Загрузка статей

1. Перейдите на вкладку **🤖 Редактор**
2. Перетащите .docx файлы в зону загрузки или нажмите для выбора
3. AI автоматически извлечет название и автора
4. Статьи автоматически отсортируются по автору (латиница → кириллица)

### Редактирование статей

- Нажмите **✏️** на карточке статьи
- Отредактируйте название и/или автора
- Нажмите **✓ Сохранить**
- Статьи автоматически пересортируются

### Генерация журнала

1. Загрузите статьи
2. (Опционально) Загрузите титульный лист и доп. страницы
3. Нажмите **🚀 Сгенерировать журнал**
4. Дождитесь завершения обработки
5. Скачайте готовый PDF

### Работа с архивом

1. Перейдите на вкладку **📚 Архив**
2. Выберите год или просмотрите все выпуски
3. Нажмите **👁️ Просмотр** для открытия PDF в браузере
4. Нажмите **⬇️ Скачать** для загрузки файла

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend (React)                     │
│                http://localhost:3000                    │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│                  Backend (FastAPI)                      │
│                http://localhost:8000                    │
└─────────────────────────────────────────────────────────┘
        │                    │                  │
        ▼                    ▼                  ▼
┌───────────────┐  ┌───────────────┐  ┌─────────────────┐
│  PostgreSQL   │  │  OpenRouter   │  │  File Storage   │
│  (metadata)   │  │  (AI service) │  │  (PDFs, DOCX)   │
└───────────────┘  └───────────────┘  └─────────────────┘
```

---

## 📁 Структура проекта

```
autoredactor/
├── backend/                    # FastAPI backend
│   ├── app/
│   │   ├── api/routes/        # API endpoints
│   │   ├── services/          # Business logic
│   │   ├── models/            # Database & Pydantic models
│   │   ├── db/                # Database connection
│   │   ├── core/              # Configuration
│   │   └── main.py            # FastAPI app
│   ├── requirements.txt
│   └── Dockerfile
│
├── frontend/                   # React frontend
│   ├── src/
│   │   ├── pages/             # Editor & Archive pages
│   │   ├── api/               # API client
│   │   ├── types/             # TypeScript types
│   │   └── App.tsx
│   ├── package.json
│   └── Dockerfile
│
├── autoeditor.py              # Original PDF builder (still used)
├── docker-compose.yml         # Full stack deployment
└── README.md
```

---

## 🔧 API Endpoints

### Upload
- `POST /api/upload/article` - Загрузка статьи с AI-извлечением
- `POST /api/upload/template` - Загрузка шаблона (title/intro/outro)

### Articles
- `GET /api/articles?session_id=...` - Список статей
- `PATCH /api/articles/{id}` - Редактирование
- `DELETE /api/articles/{id}` - Удаление
- `POST /api/articles/sort` - Пересортировка

### Generation
- `POST /api/generate` - Запуск генерации
- `GET /api/generate/{task_id}/status` - Статус
- `GET /api/generate/{task_id}/download` - Скачать PDF

### Archive
- `GET /api/archive` - Список выпусков
- `GET /api/archive/years` - Список годов
- `GET /api/archive/{id}/view` - Просмотр PDF
- `GET /api/archive/{id}/download` - Скачать PDF

Полная документация: http://localhost:8000/docs

---

## 🎯 AI Models

По умолчанию используется бесплатная модель:
- **DeepSeek R1 Distill Llama 70B** (OpenRouter)

Для изменения модели отредактируйте `backend/app/core/config.py`:

```python
AI_MODEL = "deepseek/deepseek-r1-distill-llama-70b"  # Free
# AI_MODEL = "anthropic/claude-3.5-sonnet"          # Premium
# AI_MODEL = "openai/gpt-4"                         # Premium
```

---

## 🧪 Разработка

### Backend (FastAPI)

```bash
cd backend

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Запустите сервер
uvicorn app.main:app --reload --port 8000
```

### Frontend (React)

```bash
cd frontend

# Установите зависимости
npm install

# Запустите dev server
npm run dev
```

### Database Migrations

```bash
cd backend

# Создайте миграцию
alembic revision --autogenerate -m "description"

# Примените миграции
alembic upgrade head
```

---

## 🐛 Troubleshooting

### AI не извлекает метаданные

**Проблема**: Статьи загружаются, но название/автор не определены

**Решение**:
1. Проверьте OPENROUTER_API_KEY в .env файле
2. Убедитесь, что у вас есть кредиты на OpenRouter
3. Проверьте логи backend: `docker-compose logs backend`

### Ошибка подключения к базе данных

**Проблема**: Backend не может подключиться к PostgreSQL

**Решение**:
```bash
# Перезапустите контейнеры
docker-compose down
docker-compose up -d

# Проверьте статус
docker-compose ps
```

### Frontend не может подключиться к API

**Проблема**: Ошибки CORS или сетевые ошибки

**Решение**:
1. Проверьте nginx.conf - прокси на backend должен быть настроен
2. Убедитесь, что backend запущен: http://localhost:8000/api/health
3. Проверьте CORS_ORIGINS в backend/app/core/config.py

---

## 📊 Performance

- **Обработка**: До 100 статей за сессию
- **Генерация**: Журнал до 500 страниц < 60 сек
- **AI извлечение**: ~2-3 сек на статью
- **Размер файла**: До 50 МБ на статью

---

## 🔐 Безопасность

- ✅ HTTPS ready (добавьте reverse proxy)
- ✅ Валидация файлов (.docx, .pdf only)
- ✅ Автоочистка сессий через 24 часа
- ✅ Environment variables для secrets

---

## 📝 License

MIT License - свободно используйте для коммерческих и некоммерческих проектов

---

## 🤝 Contributing

Pull requests приветствуются! Для больших изменений сначала откройте issue.

---

## 💬 Support

- GitHub Issues: https://github.com/m34959203/autoredactor/issues
- Email: support@example.com

---

**Создано с ❤️ для редакторов научных журналов**
