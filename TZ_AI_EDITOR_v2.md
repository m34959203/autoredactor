# Техническое задание: AI-Редактор журнала

## 1. Общие сведения

### 1.1 Название проекта
**AI-Редактор** — веб-приложение на базе искусственного интеллекта для автоматизации сборки научного/профессионального журнала

### 1.2 Цель проекта
Автоматизация работы редактора журнала с использованием AI: приём статей, извлечение метаданных, сортировка, сборка в PDF, ведение архива выпусков

### 1.3 Структура продукта

```
┌─────────────────────────────────────────────────────────────┐
│                     САЙТ ЖУРНАЛА                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  Главная    │  │   Архив     │  │   AI-Редактор       │  │
│  │  страница   │  │  выпусков   │  │   (отдельное app)   │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
│                         │                    │              │
│                         ▼                    ▼              │
│              ┌──────────────────┐  ┌────────────────────┐   │
│              │ Просмотр PDF     │  │ Сборка журнала     │   │
│              │ по годам/месяцам │  │ с помощью AI       │   │
│              └──────────────────┘  └────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.4 Целевая аудитория
- Редакторы научных журналов
- Редакторы корпоративных изданий
- Издательства

---

## 2. Функциональные требования

### 2.1 Загрузка файлов

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-01 | Загрузка статей в формате .docx (drag & drop + кнопка) | P0 |
| F-02 | Множественная загрузка файлов | P0 |
| F-03 | Загрузка титульного листа (PDF, Word) | P0 |
| F-04 | Загрузка вступительных страниц 1-2 (PDF, Word) | P0 |
| F-05 | Загрузка заключительных страниц (PDF, Word) | P0 |
| F-06 | Валидация форматов файлов (.docx, .pdf) | P0 |
| F-07 | Отображение прогресса загрузки статей | P0 |

### 2.2 Управление статьями

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-08 | Список загруженных статей с названиями и авторами | P0 |
| F-09 | AI: Автоматическое извлечение названия статьи | P0 |
| F-10 | AI: Автоматическое извлечение автора статьи | P0 |
| F-11 | Ручное редактирование названия статьи | P0 |
| F-12 | Ручное редактирование автора статьи | P0 |
| F-13 | Удаление статьи из списка | P0 |
| F-14 | Предпросмотр содержимого статьи | P1 |
| F-15 | Индикация типа статьи: латиница/кириллица | P1 |

### 2.3 Сортировка

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-16 | Автоматическая сортировка по алфавиту ПО АВТОРУ | P0 |
| F-17 | Приоритет: латиница (A-Z) → кириллица (А-Я) | P0 |
| F-18 | Ручная сортировка drag & drop (опционально) | P2 |

### 2.4 Настройки журнала

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-19 | Количество отступов перед статьёй (по умолчанию 4) | P0 |
| F-20 | Формат страницы (A4, Letter) | P1 |
| F-21 | Настройка размера полей (левое, правое, верхнее, нижнее) | P1 |
| F-22 | Год и месяц выпуска (для архива) | P0 |

### 2.5 Генерация журнала

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-23 | Конвертация .docx → PDF | P0 |
| F-24 | Объединение всех частей в единый PDF | P0 |
| F-25 | Автоматическая нумерация страниц | P0 |
| F-26 | Генерация содержания с номерами страниц (по сортировке) | P0 |
| F-27 | Прогресс-бар генерации | P0 |
| F-28 | Скачивание готового PDF | P0 |

### 2.6 Предпросмотр

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-29 | Предпросмотр структуры журнала перед генерацией | P1 |
| F-30 | Предпросмотр содержания | P1 |
| F-31 | Предпросмотр готового PDF в браузере | P0 |

### 2.7 Архив выпусков (отдельная страница сайта)

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-32 | Хранение PDF выпусков постоянно | P0 |
| F-33 | Сортировка по годам и месяцам | P0 |
| F-34 | Просмотр PDF в браузере | P0 |
| F-35 | Скачивание PDF из архива | P0 |
| F-36 | Поиск по архиву (по году, месяцу) | P2 |

---

## 3. AI-функционал

### 3.1 Ключевой принцип

```
╔═══════════════════════════════════════════════════════════════╗
║  🤖 AI НЕ ИЗМЕНЯЕТ СОДЕРЖИМОЕ СТАТЕЙ                          ║
║                                                               ║
║  AI выполняет только:                                         ║
║  • Извлечение метаданных (название, автор)                   ║
║  • Определение языка (латиница/кириллица)                    ║
║  • Анализ для помощи редактору                               ║
║                                                               ║
║  Тексты статей публикуются БЕЗ ИЗМЕНЕНИЙ                     ║
╚═══════════════════════════════════════════════════════════════╝
```

### 3.2 AI-функции извлечения данных

#### AI-01: Извлечение названия и автора

```python
async def extract_metadata(self, article_text: str) -> dict:
    """
    AI извлекает название статьи и ФИО автора из текста.
    Обычно это первые строки документа.
    """
    
    system = """Ты — AI-ассистент редактора журнала.
    Извлекай метаданные из научных статей.
    НЕ изменяй и НЕ редактируй текст статьи."""
    
    prompt = f"""Извлеки из начала статьи:
1. Название статьи
2. ФИО автора (или авторов)

Текст начала статьи:
{article_text[:2000]}

Ответь строго JSON:
{{
  "title": "название статьи",
  "author": "Фамилия И.О.",
  "language": "latin" или "cyrillic",
  "confidence": 0.0-1.0
}}"""

    result = await self._request(prompt, system)
    return json.loads(result)
```

#### AI-02: Определение языка для сортировки

```python
async def detect_language(self, author_name: str) -> str:
    """Определение: латиница или кириллица (для сортировки)"""
    
    # Простая проверка первого символа фамилии
    first_char = author_name.strip()[0].upper()
    
    if 'A' <= first_char <= 'Z':
        return "latin"
    elif 'А' <= first_char <= 'Я':
        return "cyrillic"
    else:
        # Для неопределённых случаев — AI
        prompt = f"""Определи, на каком алфавите написана фамилия: "{author_name}"
        Ответь одним словом: latin или cyrillic"""
        return await self._request(prompt)
```

### 3.3 AI-ассистент редактора (опционально)

```
┌────────────────────────────────────────────────────────────────┐
│  🤖 AI-АССИСТЕНТ (помощь редактору, не изменяет статьи)       │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  💬 Чат с AI:                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Редактор: Проверь, правильно ли определены авторы      │    │
│  │                                                        │    │
│  │ AI: Проверил 15 статей. Возможные ошибки:              │    │
│  │ • Статья #3: "Иванов" — возможно "Иванов И.И."         │    │
│  │ • Статья #7: автор не найден, проверьте вручную        │    │
│  └────────────────────────────────────────────────────────┘    │
│  [Введите вопрос...                              ] [Отправить] │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 4. Нефункциональные требования

### 4.1 Производительность

| ID | Требование |
|----|------------|
| NF-01 | Обработка до 100 статей за сессию |
| NF-02 | Генерация журнала до 500 страниц < 60 сек |
| NF-03 | Максимальный размер файла статьи: 50 МБ |

### 4.2 Хранение данных

| ID | Требование |
|----|------------|
| NF-04 | Word-файлы (.docx) хранятся только на время сессии |
| NF-05 | Автоматическая очистка Word-файлов через 24 часа |
| NF-06 | PDF-файлы журналов хранятся ПОСТОЯННО в Архиве |
| NF-07 | Архив сортируется по годам и месяцам выпуска |

### 4.3 Безопасность

| ID | Требование |
|----|------------|
| NF-08 | HTTPS для всех соединений |
| NF-09 | Валидация загружаемых файлов |

### 4.4 Совместимость

| ID | Требование |
|----|------------|
| NF-10 | Браузеры: Chrome 90+, Firefox 90+, Safari 14+, Edge 90+ |
| NF-11 | Адаптивный дизайн (desktop, tablet) |

---

## 5. Архитектура

### 5.1 Стек технологий

```
┌─────────────────────────────────────────────────────────────┐
│                      Frontend                               │
│  React 18 + TypeScript + Tailwind CSS + shadcn/ui          │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      Backend                                │
│  FastAPI (Python 3.11+) + Celery (фоновые задачи)          │
└─────────────────────────────────────────────────────────────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
┌───────────────────┐ ┌───────────┐ ┌───────────────┐
│   AI Service      │ │ PDF Gen   │ │  PostgreSQL   │
│   (OpenRouter)    │ │ reportlab │ │  + S3 Storage │
│   DeepSeek R1     │ │ pypdf     │ │  (архив PDF)  │
└───────────────────┘ └───────────┘ └───────────────┘
```

### 5.2 Структура проекта

```
ai-editor/
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── ui/                 # shadcn компоненты
│   │   │   ├── FileUploader/       # Загрузка файлов
│   │   │   ├── ArticleList/        # Список статей
│   │   │   ├── ArticleCard/        # Карточка статьи
│   │   │   ├── SettingsPanel/      # Настройки
│   │   │   ├── Preview/            # Предпросмотр
│   │   │   ├── ProgressBar/        # Прогресс генерации
│   │   │   ├── AIAssistant/        # AI-чат
│   │   │   └── Layout/             # Общий layout
│   │   ├── pages/
│   │   │   ├── Editor.tsx          # AI-Редактор
│   │   │   └── Archive.tsx         # Архив выпусков
│   │   ├── hooks/
│   │   ├── api/
│   │   ├── types/
│   │   └── App.tsx
│   └── package.json
│
├── backend/
│   ├── app/
│   │   ├── api/
│   │   │   ├── routes/
│   │   │   │   ├── upload.py       # Загрузка файлов
│   │   │   │   ├── articles.py     # Управление статьями
│   │   │   │   ├── generate.py     # Генерация PDF
│   │   │   │   ├── archive.py      # Архив выпусков
│   │   │   │   └── ai.py           # AI-эндпоинты
│   │   │   └── deps.py
│   │   ├── services/
│   │   │   ├── ai_extractor.py     # AI извлечение метаданных
│   │   │   ├── ai_assistant.py     # AI-чат ассистент
│   │   │   ├── docx_parser.py      # Парсинг Word
│   │   │   ├── pdf_generator.py    # Генерация PDF
│   │   │   ├── sorter.py           # Сортировка
│   │   │   ├── journal_builder.py  # Сборка журнала
│   │   │   └── archive_service.py  # Работа с архивом
│   │   ├── models/
│   │   │   ├── article.py
│   │   │   ├── journal.py
│   │   │   └── archive.py
│   │   ├── db/
│   │   │   ├── database.py
│   │   │   └── models.py           # SQLAlchemy модели
│   │   └── main.py
│   ├── requirements.txt
│   └── Dockerfile
│
├── docker-compose.yml
└── README.md
```

---

## 6. API Endpoints

### 6.1 Загрузка файлов

```
# Загрузка статьи (с AI-извлечением метаданных)
POST /api/upload/article
Content-Type: multipart/form-data
Body: file (.docx)
Response: {
  id: "uuid",
  title: "AI-извлечённое название",
  author: "AI-извлечённый автор",
  language: "latin" | "cyrillic",
  filename: "original.docx",
  size: 12345,
  confidence: 0.95
}

# Загрузка шаблона (титул, вступление, заключение)
POST /api/upload/template
Content-Type: multipart/form-data
Body: file (.pdf | .docx), type: "title" | "intro" | "outro"
Response: { id, filename, pages, type }
```

### 6.2 Управление статьями

```
# Список статей
GET /api/articles
Response: [{
  id, title, author, language, filename, order
}]

# Обновление статьи (ручное редактирование)
PATCH /api/articles/{id}
Body: { title?, author? }
Response: { id, title, author, language, order }

# Удаление статьи
DELETE /api/articles/{id}
Response: { success: true }

# Предпросмотр содержимого
GET /api/articles/{id}/preview
Response: { text: "первые 500 слов...", pages_estimate: 5 }

# Сортировка (применить авто-сортировку)
POST /api/articles/sort
Response: [{ id, author, order }]  # отсортировано
```

### 6.3 Генерация журнала

```
# Запуск генерации
POST /api/generate
Body: {
  settings: {
    indent_lines: 4,
    page_format: "A4",
    margins: { left: 2, right: 2, top: 2, bottom: 2 },
    year: 2024,
    month: 12
  },
  article_ids: ["uuid1", "uuid2", ...],
  templates: {
    title_id: "uuid" | null,
    intro_id: "uuid" | null,
    outro_id: "uuid" | null
  }
}
Response: { task_id: "uuid" }

# Статус генерации
GET /api/generate/{task_id}/status
Response: {
  status: "pending" | "processing" | "done" | "error",
  progress: 0-100,
  current_step: "Конвертация статей (5/15)..."
}

# Скачивание PDF
GET /api/generate/{task_id}/download
Response: PDF file

# Предпросмотр структуры
GET /api/generate/preview
Body: { article_ids, templates }
Response: {
  structure: [
    { type: "title", pages: 1 },
    { type: "intro", pages: 2 },
    { type: "article", title: "...", author: "...", page_start: 3 },
    ...
    { type: "toc", page_start: 45 },
    { type: "outro", pages: 2 }
  ],
  total_pages: 48
}
```

### 6.4 Архив выпусков

```
# Список выпусков
GET /api/archive
Query: ?year=2024&month=12
Response: [{
  id, year, month, filename, pages, size, created_at, url
}]

# Список годов (для навигации)
GET /api/archive/years
Response: [2024, 2023, 2022]

# Просмотр PDF
GET /api/archive/{id}/view
Response: redirect to PDF URL

# Скачивание PDF
GET /api/archive/{id}/download
Response: PDF file

# Сохранение в архив (после генерации)
POST /api/archive
Body: { task_id, year, month }
Response: { id, url }
```

### 6.5 AI-эндпоинты

```
# Повторное извлечение метаданных
POST /api/ai/extract/{article_id}
Response: { title, author, language, confidence }

# Чат с AI-ассистентом
POST /api/ai/chat
Body: {
  message: "Проверь авторов статей",
  context: { article_ids: [...] }
}
Response: { reply: "..." }
```

---

## 7. Дизайн интерфейса

### 7.1 Цветовая схема

```
Основные цвета:
─────────────────────────────────────────
Primary:      #2563EB  (синий)
Secondary:    #64748B  (серый)
Accent:       #10B981  (зелёный)
Background:   #F8FAFC  (светло-серый)
Surface:      #FFFFFF  (белый)
Text:         #1E293B  (тёмно-серый)

Статусы:
─────────────────────────────────────────
Success:      #22C55E  (зелёный)
Warning:      #F59E0B  (оранжевый)
Error:        #EF4444  (красный)
Info:         #3B82F6  (синий)

Индикаторы языка:
─────────────────────────────────────────
Latin:        #8B5CF6  (фиолетовый)
Cyrillic:     #F97316  (оранжевый)
```

### 7.2 Макет AI-Редактора

```
┌──────────────────────────────────────────────────────────────────────┐
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │ 🤖 AI-Редактор журнала                    [Архив] [?] [⚙️]     │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌──────────────────────────┐  ┌──────────────────────────────────┐  │
│  │                          │  │                                  │  │
│  │   📁 ЗАГРУЗКА ФАЙЛОВ     │  │    📋 СТАТЬИ (15)               │  │
│  │                          │  │    Сортировка: по автору A→Я    │  │
│  │  ┌────────────────────┐  │  │                                  │  │
│  │  │                    │  │  │  ┌──────────────────────────────┐│  │
│  │  │  Перетащите .docx  │  │  │  │⋮⋮ [LAT] Anderson J.         ││  │
│  │  │  файлы сюда        │  │  │  │   "Neural Networks Study"   ││  │
│  │  │                    │  │  │  │              [✏️] [👁️] [🗑️] ││  │
│  │  └────────────────────┘  │  │  └──────────────────────────────┘│  │
│  │                          │  │                                  │  │
│  │  📎 Шаблоны:             │  │  ┌──────────────────────────────┐│  │
│  │  [✓] Титульный лист      │  │  │⋮⋮ [LAT] Brown M.K.          ││  │
│  │  [✓] Страницы 1-2        │  │  │   "Data Analysis Methods"   ││  │
│  │  [ ] Заключение          │  │  │              [✏️] [👁️] [🗑️] ││  │
│  │                          │  │  └──────────────────────────────┘│  │
│  │  ─────────────────────   │  │                                  │  │
│  │                          │  │  ┌──────────────────────────────┐│  │
│  │  ⚙️ НАСТРОЙКИ            │  │  │⋮⋮ [CYR] Иванов А.А.         ││  │
│  │                          │  │  │   "Методы оптимизации"      ││  │
│  │  Отступы: [4]            │  │  │              [✏️] [👁️] [🗑️] ││  │
│  │  Формат:  [A4 ▼]         │  │  └──────────────────────────────┘│  │
│  │  Поля:    [2] см         │  │                                  │  │
│  │                          │  │  ┌──────────────────────────────┐│  │
│  │  Выпуск:                 │  │  │⋮⋮ [CYR] Петров Б.В.         ││  │
│  │  Год:   [2024]           │  │  │   "Анализ данных"           ││  │
│  │  Месяц: [Декабрь ▼]      │  │  │              [✏️] [👁️] [🗑️] ││  │
│  │                          │  │  └──────────────────────────────┘│  │
│  └──────────────────────────┘  │                                  │  │
│                                └──────────────────────────────────┘  │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  📄 ПРЕДПРОСМОТР СОДЕРЖАНИЯ                                    │  │
│  │  ────────────────────────────────────────────────────────────  │  │
│  │  Anderson J. Neural Networks Study ...................... 3    │  │
│  │  Brown M.K. Data Analysis Methods ...................... 12    │  │
│  │  Иванов А.А. Методы оптимизации ........................ 24    │  │
│  │  Петров Б.В. Анализ данных ............................. 35    │  │
│  │                                                                │  │
│  │  Всего страниц: ~48                                            │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                                                                │  │
│  │      [  🚀 СГЕНЕРИРОВАТЬ ЖУРНАЛ  ]    [💬 AI-Ассистент]        │  │
│  │                                                                │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 7.3 Макет страницы Архива

```
┌──────────────────────────────────────────────────────────────────────┐
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │ 📚 Архив выпусков                    [AI-Редактор] [?] [⚙️]    │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌─────────────┐  ┌────────────────────────────────────────────────┐ │
│  │             │  │                                                │ │
│  │  📅 ГОДЫ    │  │  📖 ВЫПУСКИ 2024                               │ │
│  │             │  │                                                │ │
│  │  ► 2024     │  │  ┌──────────────────────────────────────────┐  │ │
│  │    2023     │  │  │  📕 Декабрь 2024                         │  │ │
│  │    2022     │  │  │  48 страниц • 15 статей • 2.4 MB         │  │ │
│  │    2021     │  │  │                     [👁️ Просмотр] [⬇️]   │  │ │
│  │             │  │  └──────────────────────────────────────────┘  │ │
│  │             │  │                                                │ │
│  │  📆 МЕСЯЦЫ  │  │  ┌──────────────────────────────────────────┐  │ │
│  │  (2024)     │  │  │  📕 Ноябрь 2024                          │  │ │
│  │             │  │  │  52 страницы • 18 статей • 2.8 MB        │  │ │
│  │  Декабрь    │  │  │                     [👁️ Просмотр] [⬇️]   │  │ │
│  │  Ноябрь     │  │  └──────────────────────────────────────────┘  │ │
│  │  Октябрь    │  │                                                │ │
│  │  Сентябрь   │  │  ┌──────────────────────────────────────────┐  │ │
│  │  ...        │  │  │  📕 Октябрь 2024                         │  │ │
│  │             │  │  │  44 страницы • 12 статей • 2.1 MB        │  │ │
│  │             │  │  │                     [👁️ Просмотр] [⬇️]   │  │ │
│  │             │  │  └──────────────────────────────────────────┘  │ │
│  │             │  │                                                │ │
│  └─────────────┘  └────────────────────────────────────────────────┘ │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 7.4 Модальное окно генерации

```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│  🔄 Генерация журнала...                           [✕]    │
│                                                            │
│  ████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░  42%        │
│                                                            │
│  Этапы:                                                    │
│  ✓ Подготовка файлов                                       │
│  ✓ Конвертация статей (15/15)                              │
│  ● Формирование содержания...                              │
│  ○ Объединение PDF                                         │
│  ○ Нумерация страниц                                       │
│  ○ Финализация                                             │
│                                                            │
│  Время: 00:24 / ~01:00                                     │
│                                                            │
│                      [ Отмена ]                            │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 7.5 Компоненты

#### Карточка статьи

```
┌────────────────────────────────────────────────────────────┐
│ ⋮⋮  [LAT]  Anderson J.                          ✏️ 👁️ 🗑️  │
│            "Neural Networks: A Comprehensive Study"        │
│            article_neural.docx • 156 KB                    │
└────────────────────────────────────────────────────────────┘

Поля:
- Индикатор языка: [LAT] фиолетовый / [CYR] оранжевый
- Автор: жирный текст (по нему сортировка!)
- Название: обычный текст в кавычках
- Файл: мелкий серый текст
- Кнопки: редактировать, просмотр, удалить
```

#### Редактирование (inline)

```
┌────────────────────────────────────────────────────────────┐
│ ⋮⋮  [LAT]  [Anderson J.____________]            ✓  ✕      │
│            ["Neural Networks: A Comprehen...]              │
│            article_neural.docx • 156 KB                    │
└────────────────────────────────────────────────────────────┘
```

---

## 8. База данных

### 8.1 Схема PostgreSQL

```sql
-- Сессии редактирования
CREATE TABLE sessions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    created_at TIMESTAMP DEFAULT NOW(),
    expires_at TIMESTAMP,  -- +24 часа
    status VARCHAR(20) DEFAULT 'active'
);

-- Загруженные статьи (временные)
CREATE TABLE articles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    session_id UUID REFERENCES sessions(id) ON DELETE CASCADE,
    filename VARCHAR(255) NOT NULL,
    title VARCHAR(500),
    author VARCHAR(255),
    language VARCHAR(10),  -- 'latin' | 'cyrillic'
    sort_order INTEGER,
    file_path VARCHAR(500),  -- путь к .docx
    created_at TIMESTAMP DEFAULT NOW(),
    ai_confidence FLOAT
);

-- Шаблоны (титул, вступление, заключение)
CREATE TABLE templates (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    session_id UUID REFERENCES sessions(id) ON DELETE CASCADE,
    type VARCHAR(20) NOT NULL,  -- 'title' | 'intro' | 'outro'
    filename VARCHAR(255),
    file_path VARCHAR(500),
    pages INTEGER
);

-- Архив выпусков (постоянное хранение)
CREATE TABLE archive (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    year INTEGER NOT NULL,
    month INTEGER NOT NULL,
    filename VARCHAR(255),
    file_url VARCHAR(500),  -- URL в S3
    pages INTEGER,
    articles_count INTEGER,
    file_size BIGINT,
    created_at TIMESTAMP DEFAULT NOW(),
    
    UNIQUE(year, month)
);

-- Задачи генерации
CREATE TABLE generation_tasks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    session_id UUID REFERENCES sessions(id),
    status VARCHAR(20) DEFAULT 'pending',
    progress INTEGER DEFAULT 0,
    current_step VARCHAR(100),
    result_path VARCHAR(500),
    error_message TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    completed_at TIMESTAMP
);
```

---

## 9. Этапы разработки

### Этап 1: Backend Core (3-4 дня)

```
[ ] Настройка FastAPI + PostgreSQL + S3
[ ] Модели данных (SQLAlchemy)
[ ] Загрузка файлов (.docx, .pdf)
[ ] Парсинг .docx (извлечение текста)
[ ] AI-сервис извлечения метаданных (название, автор)
[ ] Сортировка по автору (латиница → кириллица)
[ ] Генерация PDF из статей
[ ] Формирование содержания
[ ] Объединение в итоговый PDF
[ ] Celery задачи для генерации
```

### Этап 2: Backend Archive + API (2 дня)

```
[ ] CRUD архива выпусков
[ ] Сохранение PDF в S3
[ ] API endpoints (все)
[ ] Очистка сессий по cron (24 часа)
```

### Этап 3: Frontend Editor (3-4 дня)

```
[ ] Настройка React + Vite + Tailwind + shadcn
[ ] Компонент загрузки файлов (drag & drop)
[ ] Список статей с карточками
[ ] Inline редактирование (автор, название)
[ ] Форма настроек
[ ] Предпросмотр содержания
[ ] Интеграция с API
[ ] Прогресс генерации
[ ] Просмотр/скачивание PDF
```

### Этап 4: Frontend Archive (1-2 дня)

```
[ ] Страница архива
[ ] Навигация по годам/месяцам
[ ] Просмотр PDF в браузере
[ ] Скачивание
```

### Этап 5: AI-ассистент (2 дня)

```
[ ] Подключение OpenRouter / DeepSeek
[ ] AI-извлечение метаданных
[ ] Чат-ассистент редактора
[ ] UI чата
```

### Этап 6: Финализация (2 дня)

```
[ ] Docker-контейнеризация
[ ] HTTPS настройка
[ ] Тестирование (100 статей, 500 страниц)
[ ] Документация
[ ] Деплой
```

**Общая оценка: 13-16 дней**

---

## 10. Команды для Claude Code

### 10.1 Инициализация

```bash
# Создание структуры
mkdir -p ai-editor/{frontend,backend}

# Backend
cd ai-editor/backend
python -m venv venv
source venv/bin/activate
pip install fastapi uvicorn sqlalchemy asyncpg \
    python-multipart python-docx reportlab pypdf \
    celery redis httpx boto3

# Frontend
cd ../frontend
npm create vite@latest . -- --template react-ts
npm install
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
npm install lucide-react @radix-ui/react-dialog \
    @radix-ui/react-dropdown-menu react-dropzone \
    react-pdf axios zustand
```

### 10.2 Запуск

```bash
# Backend
cd backend && uvicorn app.main:app --reload --port 8000

# Celery
celery -A app.worker worker --loglevel=info

# Frontend
cd frontend && npm run dev

# Docker (всё вместе)
docker-compose up --build
```

---

## 11. Конфигурация

### 11.1 .env файл

```env
# Database
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/aieditor

# Redis (для Celery)
REDIS_URL=redis://localhost:6379/0

# S3 Storage (для архива PDF)
S3_ENDPOINT=https://s3.amazonaws.com
S3_BUCKET=journal-archive
S3_ACCESS_KEY=xxx
S3_SECRET_KEY=xxx

# AI (OpenRouter)
OPENROUTER_API_KEY=sk-or-v1-xxx
AI_MODEL=tngtech/deepseek-r1t-chimera:free

# App
SESSION_TTL_HOURS=24
MAX_FILE_SIZE_MB=50
MAX_ARTICLES_PER_SESSION=100
```

---

## 12. Критерии приёмки

| # | Критерий | Приоритет |
|---|----------|-----------|
| 1 | Загрузка .docx и .pdf работает | P0 |
| 2 | AI извлекает название и автора | P0 |
| 3 | Сортировка по автору (латиница → кириллица) | P0 |
| 4 | Генерация PDF с содержанием | P0 |
| 5 | 4 отступа перед статьями | P0 |
| 6 | Нумерация страниц | P0 |
| 7 | Скачивание результата | P0 |
| 8 | Архив с сортировкой по годам/месяцам | P0 |
| 9 | Просмотр PDF в браузере | P0 |
| 10 | Ручное редактирование названия/автора | P0 |
| 11 | Прогресс-бар генерации | P1 |
| 12 | Drag & drop загрузка | P1 |
| 13 | Предпросмотр содержания | P1 |
| 14 | AI-чат ассистент | P2 |
| 15 | 100 статей / 500 страниц < 60 сек | P1 |

---

*Версия ТЗ: 2.0*  
*Дата: декабрь 2024*
