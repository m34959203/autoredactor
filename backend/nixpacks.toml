[phases.setup]
nixPkgs = ["python311", "libreoffice", "libreoffice-writer"]
aptPkgs = ["libreoffice", "libreoffice-writer"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
